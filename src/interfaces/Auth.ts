export interface loginForm {
    email:string,
    password:string
}

export interface registerForm {
    name:string
    email:string
    phone:string
    password: string
    confirmPassword: string
    termsAccepted: boolean
}